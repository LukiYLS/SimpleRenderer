AUX_SOURCE_DIRECTORY(${CMAKE_CURRENT_SOURCE_DIR} OBJECT_SRCS)
AUX_SOURCE_DIRECTORY(${CMAKE_CURRENT_SOURCE_DIR}/camera CAMERA_SRCS)

MESSAGE(${OBJECT_SRCS})
MESSAGE(${CAMERA_SRCS})

add_library(object 
            ${OBJECT_SRCS}
            ${CAMERA_SRCS})